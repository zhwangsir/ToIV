"use client";

/**
 * Agent Team 模块样式已迁移至 app/styles/agent-runs.css(AgentRunView.tsx 直接 import)。
 * 本组件保留空实现以兼容 app/agent-runs/page.tsx 的既有 import,后续可随路由文件清理移除。
 */
export function AgentRunStyles() {
  return null;
}
