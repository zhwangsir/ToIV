# Admin wave6 · 收尾运营面（2026-09-23）

> 背景：四域五批已收口；用户要求管理系统继续提速，且产品主干已融合为唯一 `main`。

## 本批交付

1. `GET /api/admin/closeout-summary`：公开卡烟测分布、内置软隐藏数、封面闸（含 AUTOREFIRE）、烟测/demo 批状态。
2. `POST /api/admin/apps/bulk-public`：批量 soft-hide / 上架（≤200，审计 `app.bulk_public`）。
3. 概览页「收尾运营」卡；应用管理筛选「软隐藏 / 未测」+ 批量上下架按钮。
4. 测试：`apps/api/tests/test_closeout_admin.py`。

## 不做

- 不在 UI 直接改生产 `.env` 的 `TOIV_COVER_AUTOREFIRE`（仍走设备/部署口径）。
- 不重开产品分支。
